[defaults]
inventory = inventory.ini
# Leave host_key_checking at Ansible default (true). Accept the NAS host key once
# with `ssh` before the first playbook run.
pipelining = True

[ssh_connection]
pipelining = True
